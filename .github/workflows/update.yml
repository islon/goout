name: Update Exhibition Data

on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run scraper
        run: python scripts/main.py

      - name: Enrich districts (写入 district 字段)
        run: python scripts/enrich_districts.py

      - name: Sync mini-program fallback data
        run: python scripts/sync_miniprogram_data.py

      - name: Generate per-city ICS subscription files
        run: python scripts/generate_city_ics.py

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate cities manifest (网页/小程序统一城市清单)
        run: node scripts/generate_cities_manifest.js

      - name: Commit changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "GitHub Actions"
          git add output/ miniprogram/data/exhibitions.js miniprogram/data/venues.js
          git diff --quiet && git diff --staged --quiet || git commit -m "chore: update exhibition data & sync miniprogram"

      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main