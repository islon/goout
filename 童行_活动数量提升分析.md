# 童行 · 活动数量提升分析报告

> 分析对象：https://islon.github.io/goout/ （GitHub: islon/goout）
> 分析时间：2026-07-12
> 结论先行：**活动数量的上限，几乎完全由「数据源（爬虫/账号）覆盖率」决定。** 当前深圳靠 35 个自动爬虫做到了 624 条，而北京/上海/广州/杭州四城几乎零自动化、纯靠手工录入（各 20–30 条）。要提升活动数量，核心是「扩数据源 + 修失效 + 放开微信爬虫」。

---

## 一、项目是什么

**童行** 是一个聚焦城市亲子活动的开源日历：用 Python 爬虫抓取各城市官方场馆（图书馆/博物馆/科技馆/青少年宫/体育中心等）的活动信息，整合为标准 ICS / RSS / JSON，托管在 GitHub Pages，家长可按城市一键订阅到手机日历。

**架构（数据流向）**：

```
官方场馆网页 / 公众号文章
        │
        ├─ 35 个自动爬虫 (scripts/scraper_*.py)  ──┐
        ├─ 手动整理数据 (scripts/manual_data.json) ─┤
        └─ 微信公众号爬虫 (wechat_crawler + AI 提取) ┘
                        │
                 scripts/data_pipeline.py  (归一化 / 去重 / 分类)
                        │
           输出 exhibitions.json / .ics / .rss + 按城市分文件
                        │
                GitHub Actions 定时运行 → 推送 → GitHub Pages
```

---

## 二、活动数量现状（基于代码与仓库数据核实）

| 城市 | 线上显示量 | 真实来源 | 自动化程度 |
|------|-----------|---------|-----------|
| 深圳 | ~624 条 | 35 个爬虫 + 手动(142 条未标注，默认归深圳) + 微信 | ✅ 高（主力） |
| 北京 | ~26 条 | manual_data 中 beijing=30 条 | ❌ 纯手工 |
| 上海 | ~25 条 | manual_data 中 shanghai=22 条 | ❌ 纯手工 |
| 广州 | ~23 条 | manual_data 中 guangzhou=19 条 | ❌ 纯手工 |
| 杭州 | ~30 条 | manual_data 中 hangzhou=13 条 | ❌ 纯手工 |

**关键事实（已核实）**：
- `scripts/manual_data.json` 共 226 条，其中 142 条未标注城市（默认归深圳），四城仅 84 条（北京30/上海22/广州19/杭州13）。
- `scripts/wechat_accounts.json` **配置了 56 个公众号**，但 `data_pipeline.fetch_wechat_activities(max_accounts=10, max_articles_per_account=3)` 只爬**前 10 个**（恰好全是深圳市级馆），每个号仅 3 篇文章。
- `REAL_SCRAPERS` 实际接入 **35 个**爬虫；另有 `scraper_bawt.py` 文件已写好却**未接入**流水线。
- 仓库 `output/` 中 `balib_exhibitions.json`（宝安图书馆）与 `octohbay_exhibitions.json`（湾区之眼）均为 **2 字节空 `[]`** —— 这两个爬虫失效，零产出。

---

## 三、为什么活动数量上不去（核心瓶颈）

1. **数据源覆盖极不均衡**：深圳高度自动化，四城几乎无自动爬虫，完全依赖人工录入，且人工录入量很小（每城 13–30 条）。
2. **微信公众号爬虫严重欠利用**：56 个账号只爬 10 个、每号 3 篇，且全部是深圳账号——四城零公众号覆盖。微信提取还依赖 `OPENAI_API_KEY`/`ANTHROPIC_API_KEY` 秘钥，未配置时退化为弱启发式，产出会骤减。
3. **部分已接入爬虫失效**：宝安图书馆、湾区之眼 产出为空，等于白接。
4. **已写好未接入**：`scraper_bawt.py`（宝安文体类）写了却没加进 `REAL_SCRAPERS`。
5. **大量已配置场馆根本没有爬虫**：`config.py` 里配了 URL/名称但无对应爬虫的深圳场馆约 30+ 个，尤其 6 个区图书馆（福田/罗湖/龙岗/龙华/坪山/大鹏）和多个市级核心场馆（深圳博物馆、深圳科学技术馆、深圳音乐厅、深圳滨海艺术中心、市少年宫、市文化馆、市体育中心）。
6. **更新频率与提交范围有冗余**：两套 workflow 重叠——`update-data.yml` 每 2 天只提交全量 3 个文件；`update.yml` 每周日才 `git add output/` 提交含按城市文件。结果是按城市订阅数据最多滞后 7 天。

---

## 四、提升活动数量的具体建议（按 ROI 排序）

### A 组 · 立竿见影（低工作量，当天可上）

| 动作 | 改动点 | 预计增量 |
|------|--------|---------|
| 修复失效爬虫 | 排查 `scraper_balib.py`、`scraper_bayarea_eye.py` 为何返回空（多半是官网改版/接口变了），恢复后重接 | 宝安图书馆单馆月增 30–80 条；湾区之眼月增 10–30 条 |
| 接入孤儿爬虫 | 在 `data_pipeline.py` 的 `REAL_SCRAPERS` 加入 `scraper_bawt` | +1 个数据源 |
| 放开微信爬虫 | `max_accounts=10 → 56`，`max_articles_per_account=3 → 8~10`；按城市分组爬（补北京/上海/广州/杭州账号）；确保 GitHub Secrets 配了 LLM Key | 微信渠道从「仅深圳前10号×3篇」扩到「56号×8~10篇」，量级翻数倍 |
| 合并 workflow | 两套合一，每日一次，`git add output/`（含按城市文件） | 消除最多 7 天滞后，按城市订阅不再「看起来很少」 |

### B 组 · 显著增量（中工作量，复制已有模式）

- **补齐深圳 6 个区图书馆爬虫**：福田/罗湖/龙岗/龙华/坪山/大鹏。区图书馆是亲子活动密度最高的来源之一，每馆每月可贡献 30–60 条 → 深圳总量有望再增 200–360 条。
- **补齐深圳市级核心场馆爬虫**：深圳博物馆、深圳科学技术馆、深圳音乐厅、深圳滨海艺术中心、市少年宫、市文化馆、市体育中心。这些场馆活动量大且稳定。

> 做法：直接参照现有 `scraper_nslib.py` / `scraper_szlib.py` 的模板，一个新场馆约 0.5–1 天即可完成（请求页面→解析列表→归一化为统一字段）。

### C 组 · 量级跃升（高工作量，但回报最大）

- **为北京/上海/广州/杭州建立自动爬虫体系**：照搬深圳的 33 爬虫模式，针对各城主流场馆（如北京：中国科技馆/首博/国博/国家大剧院；上海：上海科技馆/上博/天文馆；广州：广东省博/广东科学中心；杭州：浙博/良渚/杭图）逐个写爬虫。
  - 预期：四城从当前各 ~20–30 条跃升到各 **100–300+ 条**，整体活动量翻 2–3 倍。
- **建立「半自动数据源贡献」机制**：维护一份「场馆官网 URL + 列表选择器」清单，社区/志愿者填表提交后自动生成脚手架爬虫，降低拓展门槛。

### D 组 · 机制与产品层面（可持续增长）

- **数据源健康度看板**：已有 `data-review.html`，把「产出为 0 的爬虫」做成红色告警，防止静默失效（宝安图书馆就是例子）。
- **招募城市志愿者**：尤其四城，建立「每周补录本地场馆活动」的轻量流程。
- **增加「用户提交活动」入口**：现有飞书表单只收反馈，可加一个「提交活动」表单（填场馆/时间/链接），审核后入 `manual_data.json`。
- **第三方活动平台作线索源（需谨慎）**：活动行/大麦/大众点评等可作「发现线索」，但坚持项目「真实可溯源」原则——只收录能回链官方渠道的活动，不收录纯第三方聚合页。

---

## 五、优先级路线图

```
第 1 步（本周）   修失效 + 接孤儿 + 放开微信爬虫 + 合并 workflow
                  → 预计净增数十~上百条，且四城微信覆盖打开

第 2 步（2–4 周） 补齐深圳 6 区图书馆 + 7 个市级核心场馆爬虫
                  → 深圳总量 +200~360，验证模板可复制

第 3 步（1–2 月） 为四城建自动爬虫体系
                  → 四城各 100~300+，整体量级跃升

第 4 步（持续）   社区贡献机制 + 用户提交入口 + 健康度看板常态化
                  → 增长可持续、不再依赖单人维护
```

---

## 六、动手前必看的关键代码位置

| 文件 | 作用 | 改哪里 |
|------|------|--------|
| `scripts/data_pipeline.py` | 编排核心 | `REAL_SCRAPERS`（接入列表）、`fetch_wechat_activities(max_accounts=10, max_articles_per_account=3)` |
| `config.py` | 场馆 URL 与中文名 | 新增场馆先在这里加 `XXX_URL` / `XXX_NAME` |
| `scripts/wechat_accounts.json` | 56 个公众号配置 | 补充四城账号；当前只被爬前 10 个 |
| `scripts/manual_data.json` | 手工录入数据（226 条） | 四城活动主要来源 |
| `scripts/scraper_*.py` | 各场馆爬虫（现有 36 个文件，35 个接入） | 参照模板新增；修复 `balib`/`bayarea_eye` |
| `.github/workflows/*.yml` | 定时更新 | 合并为每日、`git add output/` |

---

## 七、一句话总结

> 童行不缺架构，缺的是「数据源」。把微信爬虫从 10 个账号放开到 56 个、修好 2 个失效爬虫、再把深圳缺失的区图书馆/市级馆补齐，短期就能多出几百条；而真正的量级跃升，在于把深圳的「自动爬虫模式」复制到北京/上海/广州/杭州四城。
